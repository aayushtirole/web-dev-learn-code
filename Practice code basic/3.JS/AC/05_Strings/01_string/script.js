// Strings 

let str = "Hello World";

console.log(str.length);// length property

// indexing

console.log(str[0]);   // indexing start from and can access by index

// last character access
// console.log(str[str.length - 1]);  // last character access by length -1 index
console.log(str[str.length - 1]);